{
  "name": "valeria-seguridad-alimentaria",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node scripts/build-embeddings.mjs",
    "rebuild": "rm -f data/embeddings.json && node scripts/build-embeddings.mjs"
  },
  "dependencies": {
    "openai": "^4.59.1",
    "pdfjs-dist": "^4.7.76"
  }
}
